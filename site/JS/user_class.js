export class User {
    constructor(id_user, pseudo, email, passwd, id_imagepp, theme) {
        this.id_user = id_user
        this.pseudo = pseudo
        this.email = email
        this.passwd = passwd
        this.id_imagepp = id_imagepp
        this.theme = theme
    }
}